import React from 'react';

type Props = {};

const SpLoginLayout = ({ children }: { children: React.ReactNode }) => {
  return <div className='sa_login'>{children}</div>;
};

export default SpLoginLayout;
